apiVersion: batch/v1
kind: CronJob
metadata:
  name: backup-verify
  labels:
    app.kubernetes.io/name: backup-verify
    app.kubernetes.io/component: backup
spec:
  schedule: "0 6 * * 0"  # Weekly on Sunday at 06:00 UTC
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 7200
      template:
        spec:
          serviceAccountName: {{ .Values.serviceAccount.name }}
          restartPolicy: OnFailure
          {{- with .Values.nodeSelector }}
          nodeSelector:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tolerations }}
          tolerations:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: backup-verify
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              command: ["/scripts/backup-verify.sh"]
              env:
                - name: DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.postgres.secretName }}
                      key: url
                - name: VERIFY_DATABASE_URL
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.postgres.secretName }}
                      key: verify-url
                - name: S3_BACKUP_BUCKET
                  value: {{ .Values.s3.bucket | quote }}
                - name: S3_BACKUP_PREFIX
                  value: "{{ .Values.s3.prefix }}/postgres"
                - name: NOTIFICATION_WEBHOOK
                  value: {{ .Values.notification.webhookUrl | quote }}
                - name: AWS_DEFAULT_REGION
                  value: {{ .Values.s3.region | quote }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
              volumeMounts:
                - name: scripts
                  mountPath: /scripts
          volumes:
            - name: scripts
              configMap:
                name: backup-scripts
                defaultMode: 0755
